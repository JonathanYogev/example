apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-client
  labels:
    app: {{ .Chart.Name }}-client
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .Chart.Name }}-client
